groups:
  default:
    options:
      default: true
    permissions:
    - modifyworld.*
  moderator:
    options:
      default: false
    permissions:
    - *
  admin:
    options:
      default: false
    permissions:
    - *
schema-version: 1
